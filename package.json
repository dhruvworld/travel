{
  "scripts": {
    "build": "next build",
    "postbuild": "next-sitemap",
    "prebuild": "prisma generate",
    "lint": "next lint",
    "clean": "rm -rf .next out",
    "clear-cache": "rm -rf .netlify/.cache"
  }
}